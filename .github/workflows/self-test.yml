name: Test Self

on:
  pull_request:
    branches: ['main']
  workflow_dispatch:

permissions: {}

jobs:
  create-assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Compile sketches
        uses: ./
